use bevy::prelude::*;

pub struct CombatPlugin;

impl Plugin for CombatPlugin {
    fn build(&self, _app: &mut App) {
        // stub — registers BiomeArcher, Health components + combat systems
    }
}
