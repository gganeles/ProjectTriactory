# `client/assets/fonts`

Typefaces used by `client/src/ui` (HUD numbers, dev tree labels, biome panel, lobby screens).

Keep it to the minimum set that covers the target languages at launch; each additional weight
or family is extra download/bundle size on mobile. If localization is added later, verify
glyph coverage here before shipping new languages.
